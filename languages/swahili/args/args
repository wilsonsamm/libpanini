(df argument
	(call nounphrase))
(df argument
	(call pronoun))

(df subject
	(into (theta subject) call argument))

(df object
	(into (theta object) call argument))

;(for theta (patient agent)
;	(df object
;		(theta (object .theta))
;		(seme -.theta)))

